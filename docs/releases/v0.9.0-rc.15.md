# v0.9.0-rc.15

日期：2026-09-14。分支：`codex/release-v0.9.0`。

## 改动

- 上架管理列表和库存视图中的「修改/编辑」改为紧凑弹窗，可就地修改平台售价、币种和上架日期。
- Listing 详情页保留查看信息，移除占据整页宽度的编辑卡片，并在页头提供相同弹窗入口。
- 仅在上架日期实际变化时更新该字段；只修改售价或币种时保留原始上架时间。无效日期会被拒绝。

## 验证

- TypeScript 类型检查、ESLint 和生产模式 Next.js 构建通过；构建仅保留仓库既有的未使用变量警告。
- 隔离 PostgreSQL 测试库中的 Listing 相关 11 项测试通过，包含上架日期更正、原始时间保留与无效日期拒绝。
- 在隔离测试库登录实际页面，桌面与手机截图核对弹窗尺寸和字段；实际修改上架日期后列表立即显示新日期。
- 本版本无依赖或数据库结构变化。

## 部署结果

2026-09-14 已部署至香港 ERP 测试实例 `i-j6ca0c7fir70xwtdr9pt`，入口：<https://erp-test.lianyuapt.com/listing>。

- 应用提交 `8652b6e7ecf2ab76be99c1ddb0b65bdebdba364b`；标签 `v0.9.0-rc.15`。服务器下载固定提交源码，并对归档做 SHA-256 校验。
- 发布前备份 `/opt/erp-v2/runtime/backups/pre-rc15-20260914T0710Z` 包含数据库与附件，校验和检查通过；rc.14 镜像和发布目录保留供回退。
- 新镜像先以独立容器运行，live/ready 均返回 rc.15、正确提交号及数据库 `ok`，之后切换 app 和 scheduler。外网 HTTPS live/ready 也返回相同版本。
- 当前发布目录 `/opt/erp-v2/releases/8652b6e7ecf2ab76be99c1ddb0b65bdebdba364b`，应用容器健康。

依赖清单及数据库迁移均未改变，服务器沿用 rc.14 已验证的依赖与运行基础镜像，编译并加入 rc.15 的 Next.js 构建产物；本次没有执行数据库迁移。
