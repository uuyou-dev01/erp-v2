# ERP v0.9.0 Beta release record

Status: release candidate (`0.9.0-rc.2`)
Target timezone: `Asia/Shanghai`
Target database: PostgreSQL 17
Target runtime: Alibaba Cloud ECS, mainland China, 2 vCPU / 4 GB or larger

This document becomes the immutable production release record only after every
gate below passes. Until then, blank production values and unchecked gates are
intentional blockers.

## Release identity

| Field                      | Value                         |
| -------------------------- | ----------------------------- |
| Version                    | `0.9.0-rc.2`                  |
| Release Git SHA            | pending                       |
| Image tag                  | pending (`v0.9.0-rc.2-<sha>`) |
| UAT deployment time        | pending                       |
| Production deployment time | pending                       |
| Annotated Git tag          | pending (`v0.9.0`)            |

The deployed UI and `/api/health/ready` must report the same version and full
Git SHA as the immutable image label. A mismatch blocks DNS cutover.

## Production identity and data boundary

- Owner: `youxinnan@gmail.com`
- Organization: `卖卖小东西`
- Store: `店铺1`
- Fixed retention cutoff: `2026-08-21 00:00:00+08:00`
- Source: a user-supplied PostgreSQL backup; the local `erp` database is a
  contaminated incident artifact and must not be used as a migration source.

Historical master data required by retained transactions remains in scope,
including referenced SKUs, locations, platforms, categories, memberships,
permissions, exchange rates and fee rules. Cleanup is applied only to a restored
copy after a reviewed manifest and invariant checks.

## Feature scope

- Manual one-time account invitations and password-reset links; no public signup or SMTP.
- Multiple organizations/stores per account with role and object-scope access controls.
- Enterprise connections, service agreements, directed offers, fulfillment,
  settlement, wallet and workload collaboration.
- Purchase, receiving, inspection, consolidation, inventory, listing, sale and shipping flows.
- Public catalog images and authenticated private business evidence.
- Notification deep links, read/processed state and idempotent outbox delivery.

## Database migrations

The release contains the full checked-in Prisma migration chain. Before release,
record the exact migration list and the successful `prisma migrate deploy` log
from a new empty PostgreSQL 17 database under the acceptance evidence directory.

Do not use `prisma db push`, `prisma migrate dev`, or the general demo seed in
UAT or production.

The complete chain contains 60 migrations. The collaboration and release
hardening migrations introduced since the August release baseline are:

```text
20260815120000_account_organization_collaboration
20260815153000_location_fulfillment_roster
20260815170000_narrow_warehouse_roster_access
20260815171000_service_agreement_counterparty_acceptance
20260815172000_offer_visibility_connection_source
20260816093000_dynamic_work_records
20260816100000_backfill_work_records
20260816143000_work_relationship_snapshots
20260817110000_collaboration_protocol_foundation
20260817120000_backfill_open_shipping_dispatches
20260817121000_collaboration_return_event
20260817122000_collaboration_response_history_index
20260827023000_logistics_cost_ledger
20260828090000_auth_connection_production
20260828090000_unified_asset_records
20260828100000_service_agreement_lifecycle
20260828230000_resale_listing_idempotency
```

The shipping-dispatch backfill was corrected before publication to use the
existing `(requestId, idempotencyKey)` uniqueness constraint. The resulting
chain was reapplied from zero to PostgreSQL 17.2 after the final migration edit;
see `db-assertions/empty-postgres17-migration.md`.

## Security changes

- Production self-signup defaults to disabled.
- Sessions bind to user identity, account state, expiry and session version.
- Password changes, administrative resets and account suspension invalidate old sessions.
- Production startup rejects development identities, demo passwords and missing/default secrets.
- Scheduled FX, notification and retention endpoints require independent secrets.
- Private assets require authenticated, tenant-scoped download authorization.
- Production dependency audit must contain zero critical/high findings.

## Acceptance evidence

Canonical evidence lives in [`../testing/releases/v0.9.0/`](../testing/releases/v0.9.0/):

- `acceptance-report.md`
- `screenshot-index.md`
- `screenshots/`
- `traces/`
- `db-assertions/`

The primary release reviewer must inspect every screenshot and database assertion.
A Playwright exit code alone is not acceptance.

## Deployment outline

1. Verify the original backup checksum and restore it to `erp_recovery_v090`.
2. Generate and approve the cleanup manifest; apply it transactionally to a clone.
3. Dump the cleaned database and prove it restores into a new PostgreSQL 17 database.
4. Build one immutable image labeled `v0.9.0-<sha>` from the release commit.
5. Deploy UAT, run the complete screenshot matrix and close all P0/P1 findings.
6. Stop UAT and deploy the same image with the cleaned production backup.
7. Verify read-only production smoke checks, then switch DNS.

## Rollback

1. Stop application and scheduler writes.
2. Preserve a diagnostic dump and asset snapshot from the failed release.
3. Restore the pre-release immutable database dump and corresponding asset snapshot.
4. Redeploy the prior immutable image tag; never retag `latest`.
5. Run readiness, login, permission, report and private-asset read-only checks.
6. Record the incident and rollback SHA in this release document and `CHANGELOG.md`.

Database migrations that cannot be safely reversed require forward repair against
a restored copy. Never improvise destructive SQL on the only production database.

## Release gates

- [ ] User backup checksum/version/time recorded and restore verified.
- [ ] Cleanup manifest approved and all ledger/stock/money/FK invariants pass.
- [x] Every migration is included in the release candidate and the 60-migration chain deploys to an empty PostgreSQL 17 database.
- [x] Typecheck, 503 unit/application tests and production build pass after a clean `npm ci`.
- [x] Production dependency audit reports zero vulnerabilities.
- [x] Current isolated Playwright suite passes 54/54 under a production build with one worker.
- [ ] Screenshot matrix passes with no open P0/P1.
- [ ] Ten concurrent sessions plus one OCR/scrape job complete without OOM or sustained exhaustion.
- [ ] Database and asset backup are restored successfully in a drill.
- [ ] UAT and production image digest/SHA match.
- [ ] Alibaba Cloud security group, TLS, monitoring and backup alerts are verified.
- [ ] Version changed to `0.9.0`, release record finalized and annotated tag created.
