# v0.9.0-rc.16

日期：2026-09-14。分支：`codex/release-v0.9.0`。

## 改动

- 「我的任务」发货列表改为紧凑摘要，只显示商品、SKU、件数、状态和时效；订单号、仓库及详细时间保留在任务详情中。
- 页面进入时不再自动展开第一项。点击摘要才在下方显示详情，再次点击可收起；手机端点选后会滚动到详情。带 `task` 参数的链接仍会直接打开指定任务。
- 桌面端未选任务时显示选择提示；仓库工作台原有的详情抽屉和任务操作保持可用。
- 更新端到端测试以检查手机端摘要高度、收起和展开行为，并收窄几个已有流程测试的元素定位。

## 验证

- TypeScript 类型检查、ESLint、Prettier 和生产模式 Next.js 构建通过。
- 隔离 PostgreSQL 环境中的仓库协作源测试 4 项通过；外部仓库协作与隐私保护端到端测试 2 项通过。
- 使用实际页面在 390px 手机宽度下截图核对：[收起的任务列表](../testing/releases/v0.9.0/screenshots/10-04a-compact-mobile-task-list.png)、[点选后的详情](../testing/releases/v0.9.0/screenshots/10-04b-mobile-task-detail.png)。
- 本版本无依赖或数据库结构变化。

## 部署结果

2026-09-14 已部署至香港 ERP 测试实例 `i-j6ca0c7fir70xwtdr9pt`，入口：<https://erp-test.lianyuapt.com/collaboration/tasks>。

- 应用提交 `4d9691a76c89d8570fa256b2fb0d1525bc5e4425`，标签 `v0.9.0-rc.16`。服务器下载固定提交源码并校验归档 SHA-256。
- 发布前备份 `/opt/erp-v2/runtime/backups/pre-rc16-20260914T074157Z`，数据库和附件校验均通过；上一版镜像与发布目录保留供回退。
- 新镜像先以独立容器验证 live/ready，均返回 rc.16、正确提交号和数据库 `ok`，之后切换应用与调度器。外网 HTTPS live/ready 也返回相同版本。
- 当前发布目录 `/opt/erp-v2/releases/4d9691a76c89d8570fa256b2fb0d1525bc5e4425`；应用容器健康，应用和调度器重启次数均为 0。

依赖清单及数据库迁移均未改变，服务器沿用 rc.15 已验证的依赖与运行基础镜像，编译并加入 rc.16 的 Next.js 构建产物；本次没有执行数据库迁移。
