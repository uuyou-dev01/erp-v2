# v0.9.0-rc.17

日期：2026-09-16。分支：`codex/release-v0.9.0`。

## 改动

- 通知列表把销售订单的客户姓名提升为第一识别信息，同时展示销售平台及平台订单号或系统订单号；已有通知会通过关联订单补齐这些信息。
- Listing 上架管理把状态汇总、地区、平台、搜索、风险与排序合并进同一个表格容器，减少筛选区与表格之间的空白和重复边框。
- 筛选结果为空时仍保留完整筛选表头，用户可以直接调整条件，不会陷入只有空状态、无法返回的页面。

## 验证

- TypeScript 类型检查、Prettier 和生产模式 Next.js 构建通过；构建只保留仓库既有的未使用变量警告。
- 使用本地真实页面分别检查 Listing 和通知页，确认表头合并、客户姓名层级、平台订单信息及现有操作均正常。
- 本版本无依赖或数据库结构变化。

## 部署结果

2026-09-16 已部署至香港 ERP 测试实例 `i-j6ca0c7fir70xwtdr9pt`，入口：<https://erp-test.lianyuapt.com>。

- 应用提交 `6460157f1884cf051b1014d3a4e2a6e9397073e4`，标签 `v0.9.0-rc.17`；服务器接收固定提交归档，SHA-256 为 `9ef3699a9b925ac21786679ab62abef58e8a0cffcae951465802abe65bd84706`，校验通过。
- 发布前备份 `/opt/erp-v2/runtime/backups/pre-rc17-20260916T014719Z` 包含数据库与附件，三个校验项全部通过；数据库容器未重启，未执行迁移或业务数据写入。
- 新镜像先以不暴露公网的临时容器验证，live/ready 均返回 rc.17、正确提交号和数据库 `ok`；Prisma 只读状态检查确认 64 个迁移全部为最新。
- 当前发布目录 `/opt/erp-v2/releases/6460157f1884cf051b1014d3a4e2a6e9397073e4`；应用容器健康，应用和调度器重启次数均为 0，外网 HTTPS live/ready 与内部结果一致。
- 发布前仅清理未使用的 rc.11–rc.14 Docker 镜像和可重建缓存，保留 rc.16 与 rc.15 回退镜像；数据库、附件、备份和发布目录均未删除。磁盘使用率由 91% 降至构建完成后的 72%。

依赖清单及数据库迁移均未改变，服务器复用 rc.16 已验证的依赖与运行基础镜像，编译并加入 rc.17 的 Next.js 构建产物。
