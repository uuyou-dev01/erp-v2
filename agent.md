# Agent 工作准则

本项目是 ERP 相关系统。用户可能不了解 ERP 的标准模块、术语和行业表达，但用户清楚自己的真实业务流程、人员协作方式、数据流转方式和经营目标。

当用户描述需求或下达命令时，不要机械照搬用户使用的词汇。用户的用词可能不准确、不完整，甚至与 ERP 标准概念不一致。Agent 需要以产品经理的视角理解用户真正想解决的问题，先识别业务场景、流程节点、角色权限、数据来源、审批关系、异常处理和最终产出，再用专业的 ERP 产品语言和工程实现方式进行分析、设计与开发。

开发时应遵循以下原则：

1. 优先理解业务本质，而不是纠正用户术语。
2. 将用户口语化的流程描述转化为清晰的产品需求、数据模型、页面交互、权限规则和系统流程。
3. 在信息不足时，基于 ERP 常识和当前项目结构做谨慎推断；如果关键业务规则会影响数据安全、财务结果、库存准确性或权限边界，必须明确提出问题或保守实现。
4. 设计功能时同时考虑业务可用性、系统安全性、数据一致性、审计追踪和长期可维护性。
5. 不只完成表面功能，还要关注系统能否稳定运转，包括错误处理、边界情况、并发修改、权限校验、数据校验和后续扩展。
6. 对涉及用户、客户、供应商、库存、订单、资金、审批等核心数据的功能，默认采用更严格的安全和校验策略。
7. 输出方案或实现代码时，应尽量使用用户能理解的业务语言解释，同时在代码和架构中保持专业、清晰、可维护。

如果用户的表达存在歧义，Agent 应主动将其翻译成可能的产品含义，并说明采用的理解。目标不是让用户学习 ERP 术语，而是让系统准确承载用户的真实业务。

## 固定部署拓扑（长期约束）

- ERP 测试域名固定为 `erp-test.lianyuapt.com`。项目只是借用公寓业务持有的 `lianyuapt.com` 域名，不代表 ERP 部署在公寓或酒店服务器上。
- ERP 实际运行在阿里云香港 ECS `i-j6ca0c7fir70xwtdr9pt`，公网 IP `47.243.87.184`。选择香港节点是为了同时服务东京与中国内地用户。
- 阿里云杭州 ECS `i-bp1ckvizcsym89272kyu`，公网 IP `47.98.181.237`，仅用于酒店系统。不得把 ERP 域名、容器、数据库、反向代理或发布文件部署到该实例。
- ERP 的升级、备份、数据库迁移、Nginx/HTTPS 和健康检查都必须以香港实例为目标；任何服务器操作前先核对实例 ID、地域与公网 IP。
- 详细部署边界见 `docs/deployment/hosting-topology.md`。
